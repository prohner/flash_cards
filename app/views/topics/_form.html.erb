<%= simple_form_for [@subject, @topic], :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :topic_name %>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                subject_topics_path, :class => 'btn' %>
  </div>
<% end %>
